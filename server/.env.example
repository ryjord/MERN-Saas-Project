# MongoDB connection string. A free Atlas cluster is enough.
MONGO_URL=mongodb+srv://user:password@cluster.mongodb.net/dashboard

# Port for local development. Vercel sets this itself in production.
PORT=9000
